{
  "name": "loadtesting",
  "description": "Serverless load testing in AWS using Invokust with an ElasticSearch backend.",
  "version": "0.1.0",
  "dependencies": {
    "serverless-dotenv-plugin": "^2.1.1"
  },
  "devDependencies": {
    "serverless-python-requirements": "^4.3.0"
  }
}
